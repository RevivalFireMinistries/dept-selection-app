fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
python-dotenv==1.0.0
jinja2==3.1.3
openpyxl==3.1.2
python-multipart==0.0.6
pydantic==2.5.3
apscheduler==3.10.4
bcrypt==4.1.2
reportlab==4.1.0
pywebpush==2.0.0
# Phase 5a — Clerk auth (staff-dogfood). httpx fetches JWKS;
# python-jose[cryptography] validates RS256 JWT signatures.
httpx>=0.27.0
python-jose[cryptography]>=3.3.0
# Event registration QR codes — rendered server-side so the share sheet gets a
# real PNG file and the page needs no third-party script.
qrcode[pil]==7.4.2
