[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    integration: needs the wider RFM stack running (rfm-database et al)
filterwarnings =
    ignore::DeprecationWarning
